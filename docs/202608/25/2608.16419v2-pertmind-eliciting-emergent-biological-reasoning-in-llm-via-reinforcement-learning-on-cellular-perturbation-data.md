---
title: "PertMind: Eliciting Emergent Biological Reasoning in LLM via Reinforcement Learning on Cellular Perturbation Data"
title_zh: PertMind：通过细胞扰动数据上的强化学习激发大语言模型中的涌现生物学推理
authors: "Zhenchao Tang, Xiaogang Xu, Tianxu Lv, Jiahui Guan, Jiale Zhou, Haohuai He, Zhi Song, Hanbo Huang, Jiehui Huang, Jiafei Wu, Zhe Liu"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16419v2"
tags: ["query:virtual-cell"]
score: 9.0
evidence: 以细胞扰动图谱为环境，用强化学习训练扰动反应预测模型。
tldr: 大型语言模型可描述生物机制，但后训练依赖昂贵的人工推理轨迹。PertMind将细胞扰动图谱转化为强化学习环境，以实测基因响应为计算奖励，结合可信轨迹初始化与基因、通路、格式多级信号训练。模型仅基于正向扰动预测训练，即在未见细胞情境和多重下游任务上实现迁移，并保留通用语言能力。该方法为扩展生物推理训练提供了可扩展路径。
source: arxiv
selection_source: fresh_fetch
motivation: 大规模语言模型的后训练依赖昂贵的人工推理轨迹，缺乏可扩展的生物推理训练范式。
method: PertMind利用细胞扰动图谱为强化学习环境，以实测基因响应为奖励，结合可信轨迹初始化与基因/通路/格式多级信号训练。
result: 仅用正向预测训练便提升未知情境推理，无需任务特定后训练即迁移到逆向与双扰动等任务，并生成有效的多尺度生物表征。
conclusion: 扰动衍生的强化学习能将不断扩大的实验图谱转化为通用生物推理训练环境，为激发预训练模型中的生物推理策略提供可扩展路径。
---

## 摘要
大语言模型能够描述机制，然而可扩展的后训练仍依赖于成本高昂且人工整理的生物学推理轨迹。在此，我们表明细胞扰动图谱可以转而成为强化学习环境，其中测量的基因响应为生物学推理提供可计算的奖励。我们引入了PertMind，它将可信轨迹监督初始化与基因、通路及格式层面的强化信号相结合。仅通过前向扰动-响应预测进行训练，PertMind在未见细胞情境中改善了响应推断，同时保留了通用语言能力。它还无需特定任务的后训练即可迁移至反向扰动识别、双扰动推理、表型筛选优先级排序以及生物过程解释。PertMind进一步生成了生物学特征，这些特征在跨尺度下游任务中支持了具有竞争力的基因、细胞和供体表征。这些结果支持如下假设：对实验终点的强化能够集中预训练模型已可访问的可复用生物学策略。更广泛地说，扰动衍生的强化学习提供了一条可扩展的途径，将不断扩展的实验图谱转化为通用生物学推理的训练环境。

## Abstract
Large language models can describe mechanisms, yet scalable post-training still depends on costly, manually curated biological reasoning traces. Here we show that cellular perturbation atlases can instead become reinforcement-learning environments, where measured gene responses provide computable rewards for biological reasoning. We introduce PertMind, which combines trusted-trajectory supervised initialization with gene-, pathway-, and format-level reinforcement signals. Trained only on forward perturbation-response prediction, PertMind improved response inference in unseen cellular contexts while retaining general language capabilities. It also transferred without task-specific post-training to reverse perturbation identification, double-perturbation reasoning, phenotypic-screen prioritization, and biological-process interpretation. PertMind further generated biological profiles that supported competitive gene, cell, and donor representations across multiscale downstream tasks. These results support the hypothesis that reinforcement on experimental endpoints can concentrate reusable biological strategies already accessible to pretrained models. More broadly, perturbation-derived reinforcement learning offers a scalable route for transforming expanding experimental atlases into training environments for general-purpose biological reasoning.

---

## 论文详细总结（自动生成）

# PertMind：通过细胞扰动数据上的强化学习激发大语言模型中的涌现生物学推理

## 1. 核心问题与整体含义

- **研究动机**：大型语言模型（LLM）已经具备描述生物机制的能力，但其可扩展的后训练仍然依赖成本高昂、人工整理的生物学推理轨迹（如专家标注的思维链）。这种人工数据瓶颈限制了 LLM 在生物学推理任务上的规模化训练。
- **核心问题**：如何设计一种不依赖人工推理轨迹的可扩展后训练范式，使 LLM 能够基于实验数据自主发展出可靠的生物学推理能力？
- **整体含义**：论文提出将大规模细胞扰动图谱（cellular perturbation atlases）转化为强化学习（RL）环境，以实测基因响应作为计算奖励，从而训练模型在生物学推理中进行“试错与优化”。这一思路将不断扩大的实验数据图谱转化为通用的推理训练资源，为激发预训练模型中已有的可复用生物学策略提供了可扩展路径。

## 2. 方法论

- **核心思想**：利用细胞扰动数据构建 RL 环境：模型输出扰动后的基因响应预测，将预测与实测响应之间的差异作为奖励信号，从而无需人工标注推理轨迹即可对模型进行强化训练。
- **方法名称**：PertMind。
- **技术组成**：
  1. **可信轨迹监督初始化（Trusted-trajectory supervised initialization）**：先用少量可信的推理轨迹对模型进行监督微调，为后续 RL 提供稳定的起点，避免探索初期奖励稀疏、信号不稳定。
  2. **多级强化信号**：在基因（gene）、通路（pathway）、格式（format）三个层面分别设计奖励信号，使模型不仅关注单基因表达准确度，还能在生物学通路层面保持一致性，并输出符合规范格式的推理内容。
- **训练目标**：仅使用**前向扰动-响应预测**（给定细胞状态和扰动，预测基因表达变化）作为训练任务。
- **核心优势机制**：通过 RL 对实验终点（experimental endpoints）进行优化，能够将预训练模型中已存在但分散的、可复用的生物学推理策略集中并强化，从而在未见情境中泛化，而无需针对每个下游任务重新训练。

## 3. 实验设计

- **数据集 / 场景**：文中未明确列出具体数据集名称（如是否有 DepMap、LINCS、Perturb-seq 等）。仅提到“细胞扰动图谱”以及“未见细胞情境”。具体数据来源与规模在提供的材料中未说明。
- **Benchmark 与评估任务**：
  - 前向扰动-响应预测（训练任务）在**未见细胞情境**下的响应推断；
  - 通用语言能力保持评估；
  - **迁移任务**（无需任务特定后训练）：
    - 逆向扰动识别（给定响应推断扰动）
    - 双扰动推理（组合扰动效果预测或解释）
    - 表型筛选优先级排序
    - 生物过程解释
  - **多尺度下游表征任务**：评估 PertMind 生成的生物学特征（biological profiles）在基因、细胞、供体三个层面的表征性能，并与竞争性基线比较。
- **对比方法**：文中未给出具体基线名称，仅提及“competitive gene, cell, and donor representations across multiscale downstream tasks”，即与已有表征方法进行了竞争性比较。

## 4. 资源与算力

- **未明确说明**：提供的文本中未提及 GPU 型号、数量、训练时长、参数量、训练数据规模等具体算力信息。
- 因此无法评估训练成本的可复现性需求，但可推测其实验需要较大规模的扰动数据和一定量的 RL 训练资源。

## 5. 实验数量与充分性

- **实验覆盖范围较广**：从摘要看至少包含 6 类评估场景：
  1. 未见细胞情境下的前向响应推断；
  2. 通用语言能力保持；
  3. 逆向扰动识别；
  4. 双扰动推理；
  5. 表型筛选优先级排序；
  6. 生物过程解释；
  7. 多尺度（基因、细胞、供体）下游表征任务。
- **是否充分 / 客观 / 公平**：
  - **优点**：评估维度多样，既考核了“生物学专用能力”，也考察了“通用能力保持”，迁移任务覆盖了多种推理范式，能较好展示方法泛化性。
  - **不足**：由于缺少论文正文，无法获知具体实验设置、对照方法、统计显著性、消融实验细节（如是否对多级奖励设计进行消融、奖励权重敏感性、初始化轨迹数量等）。因此暂无法从现有信息判断实验的公平性与充分性。通常此类论文应包含消融实验和与多种基线的对比，但在当前提供的内容中未体现。

## 6. 主要结论与发现

- PertMind 仅通过前向扰动-响应预测的 RL 训练，即可在**未见细胞情境**中改善响应推断，同时不损失通用语言能力。
- 训练后的模型**无需任务特定后训练**即可迁移到逆向扰动识别、双扰动推理、表型筛选优先级排序、生物过程解释等下游任务。
- PertMind 生成的生物学特征在**基因、细胞、供体**等多尺度下游任务中具有竞争力。
- 支持假设：在实验终点上的强化能够“集中”预训练模型中已有的可复用生物学策略，使其显式可用。
- 更广泛的结论：扰动衍生的 RL 是将不断扩展的实验图谱转化为通用生物学推理训练环境的可扩展途径。

## 7. 优点

- **范式创新**：首次将细胞扰动图谱直接转化为 RL 环境，以实测响应作为奖励，绕开了人工推理轨迹成本高、难以扩展的瓶颈。
- **训练效率高**：只用前向预测一个任务进行训练，却能迁移到多个不同类型的下游任务，体现了强泛化能力。
- **多尺度奖励设计**：基因、通路、格式三层奖励结合在一起，既引导单基因级准确性，也保证通路级生物学合理性以及输出格式规范性，设计思路较为合理。
- **保留通用能力**：在提升生物推理的同时不牺牲通用语言能力，说明强化训练没有对模型原有能力造成灾难性遗忘。
- **应用潜力大**：提供了一种可以随实验图谱持续扩展的框架，具有数据规模递增的天然优势。

## 8. 不足与局限

- **信息不完整**：我们目前仅能看到论文摘要和元数据，无法获取正文中关于实验设置、数据来源、基线选择、超参数等关键细节。因此以下局限为基于现有信息的推断：
- **实验细节缺失**：未给出具体数据集名称、样本量、扰动类型、细胞系范围、评价指标等，难以复现和横向对比。
- **奖励设计的主观性**：基因/通路/格式三层奖励的具体权重和阈值如何确定？是否具有跨细胞类型/跨任务的鲁棒性？未见消融分析。
- **“可信轨迹初始化”依赖**：即使使用了 RL，监督初始化阶段仍然依赖少量人工整理的推理轨迹。这部分成本虽然比大规模后训练低，但并未完全消除人工依赖。
- **迁移能力边界不清**：摘要宣称迁移到多类任务，但未说明在“未见细胞情境”之外的“未见扰动类型”“未见组织”等更极端泛化场景下的表现。
- **与现有领域专用模型的比较缺失**：未提及与专门的扰动预测模型（如基于图神经网络、Transformer 的虚拟细胞模型）的对比，因此难以客观评价 PertMind 在专精度上的绝对优势。
- **表征质量的评价范围有限**：虽然提到基因、细胞、供体表征“有竞争力”，但未说明具体下游任务类型（如聚类、分类、关联分析）及与哪些表征方法比较，结论强度有限。
- **潜在偏差风险**：RL 奖励基于实测基因响应，如果训练数据本身存在批次效应、噪声或特定实验条件下的偏差，模型可能学会利用这些伪影而非真实生物学规律。文中未提及对数据质量控制的讨论。

（完）
