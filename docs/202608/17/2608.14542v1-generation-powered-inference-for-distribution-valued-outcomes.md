---
title: Generation-Powered Inference for Distribution-Valued Outcomes
title_zh: 基于生成的推断用于分布值结果
authors: "Yijiao Zhang, Hongzhe Li"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14542v1"
tags: ["query:virtual-cell"]
score: 8.0
evidence: 提出统计框架以改进使用生成模型预测遗传扰动细胞响应时的推断
tldr: 现代生成模型可输出分布值预测（如细胞对遗传扰动的响应），但预测并不完美。针对分布值参数，提出生成驱动推断（GPI），用函数值桥表示将Wasserstein空间推断转化为Hilbert空间均值函数估计，构建增广估计框架。所提估计器实现最优信息借用，具备相合性、渐近正态性和置信带，模拟显示效率提升且对模型误设定稳健。该框架为分布值结果推断提供通用方案，在Perturb-seq等场景有应用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14542v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1679, \"height\": 916, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1510, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 855, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1693, \"height\": 976, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14542v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1664, \"height\": 921, \"label\": \"Figure\"}]"
motivation: 生成模型的分布值预测可能不准确，需要利用这些预测而不依赖其正确性的统计推断方法。
method: 提出生成驱动推断（GPI），用函数值桥表示将非线性Wasserstein推断转化为Hilbert空间均值函数估计，并构建增广估计框架。
result: 证明GPI估计器实现最优信息借用，具备相合性、渐近正态性和同步置信带，模拟显示效率提升且对模型误设定稳健。
conclusion: 为分布值参数推断提供通用可靠的框架，在单细胞基因组学等分布值结果场景具有重要应用价值。
---

## 摘要
现代生成模型日益产生分布值的输出，例如单细胞基因组学中预测的细胞对遗传扰动的响应。虽然这些模型提供了有价值的辅助信息，但它们本质上是非完美的，因此需要统计方法在不依赖其正确性的情况下利用其预测。我们提出了生成驱动的推断（GPI），一个通用框架，利用辅助生成模型改进对分布值参数的推断。聚焦于Wasserstein重心及相关分布泛函，我们引入了一个函数值桥接表示，将非线性Wasserstein空间中的推断转化为希尔伯特空间中均值函数的估计，从而实现了类似于预测驱动推断的增强估计框架。我们开发了一族具有最优信息借用的GPI估计器，建立了相合性、渐近正态性和同时置信带，并为线性泛函和Wasserstein距离推导了有效推断。模拟研究证明了相较于仅使用标注数据方法的效率提升，以及在生成模型误设定下的稳健性能。我们使用一项K562细胞的Perturb-seq研究来阐述该框架，其中由State基础模型生成的合成扰动响应被用于改进与40S核糖体模块扰动相关的通路水平共识基因表达分布的推断。

## Abstract
Modern generative models increasingly produce distribution-valued outputs, such as predicted cellular responses to genetic perturbations in single-cell genomics. While these models provide valuable auxiliary information, they are inherently imperfect, creating a need for statistical methods that leverage their predictions without relying on their correctness. We propose generation-powered inference (GPI), a general framework for improving inference on distribution-valued parameters using auxiliary generative models. Focusing on Wasserstein barycenters and related distributional functionals, we introduce a function-valued bridge representation that transforms inference in the nonlinear Wasserstein space into estimation of a mean function in a Hilbert space, enabling an augmented estimation framework analogous to prediction-powered inference. We develop a family of GPI estimators with optimal information borrowing, establish consistency, asymptotic normality, and simultaneous confidence bands, and derive valid inference for linear functionals and Wasserstein distances. Simulation studies demonstrate efficiency gains over labeled-data-only methods and robust performance under generative model misspecification. We illustrate the proposed framework using a Perturb-seq study of K562 cells, where synthetic perturbation responses generated by the State foundation model are used to improve inference for pathway-level consensus gene expression distributions associated with perturbations of the 40S ribosome module.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义

### 研究动机与背景

- 现代生成模型（如单细胞基因组学中的基础模型、大语言模型、气候模型、数字孪生系统等）越来越多地产生分布值输出（distribution-valued outputs），例如预测细胞对遗传扰动的响应分布。
- 这些生成模型虽然提供丰富的辅助信息，但本质上是非完美的：受有限且有偏训练数据、简化假设、泛化失败等因素影响，预测可能存在系统性偏差。
- 已有统计方法（如预测驱动推断，PPI）可利用机器学习预测对有限维欧几里得参数进行偏差校正和效率增强，但无法处理分布值参数——分布值对象属于非线性 Wasserstein 空间，经典半参数工具不直接适用。
- 因此本文提出**生成驱动推断（GPI）**，一种通用框架：在分布值参数推断中利用生成模型的预测，同时不依赖其正确性，保证推断的有效性并实现效率提升。

## 方法论

### 核心思想

- **函数值桥接表示（bridge representation）**：
  - 对于 Wasserstein 重心 μ₌ 和连续参考分布 λ，定义函数值目标参数 θλ(·) = μ₌⁻¹ ∘ λ(·)，将非线性 Wasserstein 空间中的推断转化为 Hilbert 空间 L²(T;λ) 中均值函数的估计。
  - 这满足两个关键性质：(a) 具有渐近线性表示，从而可自然定义校正项（rectifier）；(b) 嵌入下游推断所需的信息。
  - 通过选择不同的参考分布 λ，可统一编码重心分位数函数、最优传输映射、位移函数、Wasserstein 距离以及各类线性和非线性分布汇总量。

### 关键技术细节

- **GP估计器构造**：
  - 将变换后的观测 Zλ = Y⁻¹ ∘ λ 与变换后的生成代理 gλ(X) = G(X)⁻¹ ∘ λ 组合为：θλ = E[Γgλ(X)] + E[Zλ − Γgλ(X)]，其中 Γ 是控制信息借用的有界线性算子。
  - 基本 GPI 估计器使用经验矩替代总体矩。
  - 交叉拟合 GPI（cross-fitted GPI）：当生成模型需要从标注数据训练时，将标注样本分成 K 折，每折用其余数据训练生成模型，然后合并各折估计。
- **最优信息借用**：
  - 定理 2 给出协方差算子意义上的最优算子 Γ*，满足 (1+ρ)Γ*Cgg = CZg，其中 ρ = lim n/N。效率增益由生成代理线性解释的 Zλ 变异性决定。
  - 特殊情形下（生成代理是条件投影），Γ* 可简化为 (1+ρ)⁻¹ I。
  - 估计最优算子：基于函数主成分分析（FPCA）的积分算子和截断、岭正则化参数估计（公式 10）。
- **推断程序**：
  - 定理 3：基于高斯近似的同步置信带（SCB）。
  - 线性泛函：CI 公式（12），适用于均值、截尾均值、尾均值、位移泛函等。
  - Wasserstein 距离推断（定理 4）：W₂ 距离的相合估计，零假设下 W₂ = 0 的退化极限分布，以及基于高斯过程样本的检验程序。

## 实验设计

### 模拟研究

- **数据生成**：偏态正态分布分位数曲线，带位置/尺度/高阶形状基函数（ϕ₁,ϕ₂,ϕ₃）的逐个体分位数函数；生成代理带有偏置和四种符号对齐模式（+++、+-+、-+-、---），三种对齐强度（强、中、弱）。
- **参数设置**：n = 20（标注样本）、m = 500（每个分布的观测细胞数）、N = 50（未标注样本），1000 次重复；B = 1000 个高斯样本用于 SCB 和 Wasserstein 检验。
- **对比方法**：Labeled（仅标注数据）、Naive GPI（Γ=(1+ρ)⁻¹I）、Scalar GPI（Γ=γI）、General GPI（估计的最优算子）。
- **评估指标**：估计误差（RMISE）、SCB 覆盖率与带宽、CI 覆盖率与长度、Wasserstein 检验的拒绝率。

### 真实数据应用

- **数据集**：Replogle 等人（2022）的 K562 Perturb-seq 数据（CRISPR 干扰 + 单细胞 RNA 测序）。
- **生成模型**：State 基础模型（transformer 架构，约 1.67 亿单细胞转录组预训练，非 K562 扰动数据微调）。
- **分析对象**：40S 核糖体模块，74 个扰动目标（21 个标注 + 53 个未标注），1,980 个响应基因。
- **基准**：Full observed（全部 74 个观测分布的经验重心）、Pooled（21 个真实分布 + 53 个 State 生成分布直接合并）、Labeled、三种 GPI 方法。
- **分析内容**：分位数位移曲线、均值/中心区/上尾位移泛函的推断、基因聚类与 GO 富集分析。

## 资源与算力

- **论文明确说明的算力信息有限**：未报告使用的 GPU 型号、数量或训练时长。
- 真实数据实验中使用了预训练的 State 基础模型，但模型的训练成本不在本文范围内，也未披露。
- 模拟实验（1000 次重复 × 12 种设置，B=1000）和真实数据推断均需要一定计算资源，但论文未提供具体硬件细节。

## 实验数量与充分性

### 实验数量

- **模拟**：12 种生成代理设置（4 种符号对齐 × 3 种强度）× 3 类指标（估计、SCB、Wasserstein 检验）；补充材料进一步覆盖了线性位移泛函、不同的未标注样本量、不同样本量（更小和更大）的敏感性分析。
- **真实数据**：1,980 个基因，基于 FPC 的基因聚类（8 类），GO 富集分析，代表性基因个案展示。
- **消融/对比**：Naive vs Scalar vs General GPI，以及 Pooled 和 Labeled 基准。

### 充分性与客观性评估

- 实验设计较全面，覆盖了生成代理的不同对齐方向和强度，以及混合对齐情境，验证了方法在不同条件下的有效性。
- 模拟中固定 n, m, N = (20, 500, 50)，补充材料提供了其他样本量，但核心模拟的未标注样本量相对较小（N=50），与真实应用中“大量未标注数据”的设定存在一定差距。
- 真实数据基准（Full observed）基于同一实验数据，评估方式合理，但 74 个扰动仍然有限。

## 论文主要结论与发现

1. GPI 在分布值参数推断中能够有效利用生成模型信息，实现效率提升并保持推断有效性。
2. General GPI（最优算子）在大多数对齐模式下表现最稳健；Scalar GPI 在均匀对齐时可略优；Naive GPI 在负对齐或混合对齐时可能劣于仅标注数据方法。
3. 理论上证明了 GPI 估计器的相合性、渐近正态性、最优协方差效率，以及 SCB 和线性泛函/ W₂ 距离推断的有效性。
4. 真实 Perturb-seq 分析中，GPI 位移估计揭示了基因间显著的分位数异质性，聚类结果显示出具有生物学意义的量级依赖型响应模式（如有丝分裂/染色体分离、翻译/蛋白稳态、DNA 复制起始等），表明 40S 核糖体扰动不仅改变均值表达，还显著重塑分布的尾部与中间区域。

## 优点

- **理论贡献完整**：从桥接表示、估计器构造、最优性、推断到可行估计，形成了系统的理论框架。
- **非线性问题线性化**：将 Wasserstein 空间的推断转化为 Hilbert 空间均值估计，概念优雅且实用性强。
- **鲁棒性**：对生成模型误设定保持稳健；非信息性代理不造成效率损失，信息性代理带来效率增益。
- **统一框架**：覆盖分位数函数、线性泛函、位移、传输映射、W₂ 距离等多种下游推断目标。
- **交叉拟合设计**：避免了生成模型训练与评估样本重叠带来的过拟合偏差。
- **真实数据验证充分**：K562 Perturb-seq 分析展示了从校准验证到生物学发现的完整流程。

## 不足与局限

- **算力信息缺失**：未披露 GPU 型号、数量、训练时间等资源使用情况。
- **模拟限制**：核心模拟固定未标注样本量 N=50，偏小；未标注样本量更大时的表现仅在补充材料中部分覆盖。
- **理论假设较强**：依赖 MCAR 缺失机制、参考分布 λ 已知、分布估计的收敛率假设（Assumption 1–3），实际应用中这些假设的满足程度需要检验。
- **算子估计复杂性**：General GPI 依赖 FPCA、截断水平（L, M）和岭参数（κ）的选择，实际操作中存在调参负担。
- **扩展性受限**：框架主要针对一维分布和 Wasserstein 重心；高维分布、条件分布、复杂依赖结构等场景尚未覆盖。
- **基准比较的局限**：真实数据中 Pooled 基准虽然给出更窄的置信区间，但估计偏差较大，说明直接合并生成数据存在风险；然而对 Full observed 基准结果的推广性需要更多外部验证。
- **未讨论多生成模型集成或协变量偏移场景**：当标注与未标注总体分布不一致时，方法需要额外扩展。

（完）
