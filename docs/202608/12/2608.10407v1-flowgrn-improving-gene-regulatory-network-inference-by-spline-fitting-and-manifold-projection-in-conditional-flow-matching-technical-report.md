---
title: "FlowGRN+: Improving Gene Regulatory Network Inference by Spline Fitting and Manifold Projection in Conditional Flow Matching (Technical Report)"
title_zh: FlowGRN+：通过条件流匹配中的样条拟合与流形投影改进基因调控网络推断（技术报告）
authors: "Tsz Pan Tong, Jun Pang"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10407v1"
tags: ["query:virtual-cell"]
score: 8.0
evidence: 利用条件流匹配学习单细胞动态并推断基因调控网络，是虚拟细胞建模的关键技术
tldr: 从scRNA-seq数据推断基因调控网络面临高维性和dropout挑战。现有FlowGRN方法通过条件流匹配重建轨迹，但存在时序连贯性不足且依赖人工检查的问题。FlowGRN+将样条拟合引入CFM框架生成稳定参考轨迹，并采用切空间投影缓解过冲。在BEELINE上实现更平滑的轨迹和竞争性的GRN推断性能，为CFM类模型提供实用改进方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10407v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1816, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10407v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 887, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10407v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1038, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10407v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 804, \"height\": 776, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.10407v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 928, \"height\": 528, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10407v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 787, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10407v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1530, \"height\": 841, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10407v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1545, \"height\": 840, \"label\": \"Table\"}]"
motivation: FlowGRN在时序连贯性上存在局限，且结果依赖人工检查，影响下游应用与复现。
method: 在CFM框架中集成样条拟合生成稳定参考轨迹，并将样条切线投影到数据流形局部切空间以避免过冲。
result: 在BEELINE基准测试中，FlowGRN+提升了轨迹平滑度，并保持了有竞争力的GRN推断性能。
conclusion: FlowGRN+提供了实用的scRNA-seq轨迹重建与GRN推断框架，其思路可推广至其他基于CFM的细胞动力学模型。
---

## 摘要
基因调控网络（GRNs）是理解发育和疾病过程中细胞动态及潜在机制的基础。尽管单细胞RNA测序（scRNA-seq）技术已能够在单细胞分辨率下收集大量基因表达谱，但由于高维性和数据丢失，从scRNA-seq数据推断GRNs仍然是一项重大挑战。近期，FlowGRN通过应用条件流匹配（CFM）学习细胞动态，在重建细胞轨迹和推断GRNs方面展示了令人鼓舞的结果。然而，FlowGRN在重建动态的时间连贯性方面仍存在局限性，并且依赖于人工检查，这阻碍了下游应用和可重复性。在本文中，我们提出了FlowGRN+，这是FlowGRN的改进版本，它将样条拟合整合到CFM框架中，以生成更稳定的参考轨迹用于训练，从而改善学习动态的时间连贯性。为了解决样条拟合中的过冲问题，我们进一步引入了一种投影方案，将样条切线投影到数据流形的局部切线空间。我们在BEELINE基准上评估了FlowGRN+，结果显示轨迹平滑度有所提高，同时GRN推断性能具有竞争力。FlowGRN+为从scRNA-seq数据重建细胞轨迹和推断GRNs提供了一个实用框架，本工作的见解也可能对其他基于CFM的细胞动态模型有用。

## Abstract
Gene regulatory networks (GRNs) are fundamental in understanding cellular dynamics and underlying mechanisms during development and disease. Although scRNA-seq technologies have enabled the collection of vast numbers of gene expression profiles at single-cell resolution, inferring GRNs from scRNA-seq data remains a significant challenge due to high dimensionality and dropout. Recently, FlowGRN has shown promising results in reconstructing cell trajectories and inferring GRNs by applying conditional flow matching (CFM) to learn the cell dynamics. However, FlowGRN still faces limitations in the temporal coherence of reconstructed dynamics and relies on human inspection, which hinders downstream applications and reproducibility. In this paper, we propose FlowGRN+, an improved version of FlowGRN that integrates spline fitting into the CFM framework to generate more stable reference trajectories for training, thereby improving the temporal coherence of the learned dynamics. To address overshooting in spline fitting, we further introduce a projection scheme that projects spline tangents onto the local tangent space of the data manifold. We evaluate FlowGRN+ on the BEELINE benchmark and show improved trajectory smoothness with a competitive GRN inference performance. FlowGRN+ provides a practical framework for reconstructing cell trajectories and inferring GRNs from scRNA-seq data, and the insights from this work may also be useful for other CFM-based models of cellular dynamics.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

基因调控网络（GRN）是理解细胞在发育与疾病过程中动态变化的基础。单细胞RNA测序（scRNA-seq）虽能捕捉单细胞分辨率下的基因表达快照，但由于细胞在测序时被破坏，无法追踪同一细胞随时间的变化，因此从这些静态快照中推断隐藏的GRN动态极具挑战。

- 经典方法（如GENIE3、TIGRESS、PIDC）将数据视为静态快照，丢弃了时间信息，难以捕捉GRN的动态本质。
- 基于常微分方程（ODE）的方法（如SCODE、GRISLI）虽显式建模细胞动态，但对高维、非线性细胞动态存在过度简化和可扩展性问题。
- FlowGRN 是首个将条件流匹配（CFM）用于GRN推断的模型，通过学习细胞动态重建轨迹并推断GRN，但存在三个局限：
  1. 线性插值参考轨迹在时间边界处不连续，违背细胞动态的连续性；
  2. 仅考虑相邻时间点的局部动态，缺乏长期时序结构；
  3. 依赖清晰分离的快照分布，需要人工检查聚类参数，可复现性差。

FlowGRN+ 通过样条拟合和流形投影解决上述问题，提升学习动态的时间连贯性和流形对齐程度。

### 2. 方法论

**核心思想**：FlowGRN+ 构建在多边缘流匹配（MMFM）框架之上，利用平滑样条生成更稳定的参考轨迹，缓解dropout引起的速度突变，并通过切空间投影抑制样条拟合的过冲现象。

**关键技术流程**（在log域中执行）：

1. **伪时间分箱**：利用Slingshot推断伪时间τ(x)，按细胞数均匀分为b个bin，并手动移除稳态细胞（当推断伪时间与采样时间顺序不符时）。
2. **OT耦合与样条拟合**：计算相邻bin间的dropout鲁棒最优传输（OT）耦合，采样多细胞序列，拟合平滑样条γ(τ)作为参考轨迹。样条为B-spline，目标函数平衡数据拟合与曲率正则化：
   
   min Σ∥xi − γ(τ(xi))∥² + λreg ∫∥γ″(τ)∥²dτ

3. **训练样本构造**：采样τ ~ U(0,1)，利用式(3)-(5)构造训练对(x, u)。
4. **流形投影**：对x和u分别进行投影校正（详见下述投影方案）。
5. **CFM训练**：最小化度量感知损失：
   
   L_MFM(θ) = E∥vθ(x,τ) − u(x,τ|z)∥²_G(x)，其中 G(x) = P(x) + ηI

**投影方案**：
- 通过kNN邻居N_k(x)捕获局部几何，计算RBF加权局部协方差矩阵Σ(x)；
- 采用谱滤波g_{λ0}(λ) = λ/(λ+λ0)抑制法向特征向量，得到投影矩阵 P(x) = Σ(Σ+λ0I)⁻¹；
- 构造仿射回拉算子Π(x) = N̄_k(x) + P(x)(x − N̄_k(x))，通过R次迭代将越界点拉回数据流形；
- 利用Woodbury恒等式将计算复杂度从O(g³)降至O(gk² + k³)，只需解k×k线性系统；
- 在轨迹积分时缓存kNN查询结果以加速计算。

**轨迹重建与GRN推断**：
- 训练完成后，从每个细胞的伪时间出发积分学习到的向量场重建轨迹；
- 积分使用切投影速度并在每步进行回拉校正；
- 轨迹映射回基因表达域后，用dynGENIE3推断GRN。

### 3. 实验设计

- **基准**：BEELINE基准中的7个实验数据集（人类和小鼠）：hESC、hHep、mDC、mESC、mHSC-E、mHSC-GM、mHSC-L，涵盖 "TFs+500基因" 和 "TFs+1000基因" 两种设置共14个实验场景。
- **参考网络**：BEELINE提供的参考网络未更新，作者改用DoRothEA和CollecTRI数据库获取基因调控关系。
- **对比方法**：LEAP, SCODE, GRISLI, GRNVBEM, SINCERITIES, Scribe, GENIE3, GRNBOOST2, FlowGRN。
- **评估指标**：AUPRC（全排名边恢复）、EPR（Top-k边精度，归一化随机基线）、归一化总变差TV（轨迹平滑度）。
- **消融实验**：比较原始FlowGRN（Orig.）、纯样条拟合（Spl.）、FlowGRN+（Traj.）；以及有投影与无投影（No Proj.）的设置；对比线性/三次/平滑/pchip等不同样条方法。

### 4. 资源与算力

文中对算力资源有部分说明：

- 神经网络在**NVIDIA V100 SXM2 GPU（16GB显存）**上训练，训练**1000个epoch**，batch size为128。
- dynGENIE3在**128个CPU（2.6GHz）和256GB内存**的节点上运行，使用1000棵随机森林树。
- 所有基线方法在相同平台上运行，限时48小时，使用Singularity容器保证可复现性。
- 轨迹积分使用TorchDyn中的DOPRI5求解器。
- 文中提到FlowGRN+的训练时间比FlowGRN更长（因投影方案计算开销大），但未给出具体的训练时长数值。

### 5. 实验数量与充分性

- **主实验**：14个数据集设置中的AUPRC和EPR比较，每种方法运行5次（随机种子1-5）取平均值和标准差，涵盖了对性能的统计评估。
- **消融实验**：42种设置组合（14个数据集设置×3种样条方法），评估投影方案的效果（EPR提升28/42，TV降低30/42）。
- **充分性评价**：
  - **优点**：覆盖多个真实数据集、多种基线方法、多种评估指标，且有5次重复实验对冲随机性，消融实验设计较系统（验证样条类型和投影方案的各自贡献）。
  - **不足**：结果方差较大（如mESC数据集EPR标准差达±5.81），且未报告轨迹重建保真度（如与真实轨迹的误差），仅以平滑度TV衡量。未能充分说明不同参考网络来源对结果的影响。

### 6. 主要结论与发现

1. **性能**：FlowGRN+在BEELINE实验数据集上达到顶级性能，在14个设置中有10个EPR排名前二；相比FlowGRN，EPR在10/14设置中更高，但AUPRC仅在4/14设置中更高，说明优势集中于Top-ranked边恢复。
2. **轨迹平滑性**：投影方案在28/42设置中提升EPR，在30/42设置中降低归一化TV，经验上支持投影方案改善学习动态和GRN推断性能。
3. **样条方法选择**：平滑样条的平均EPR排名（1.64）优于pchip（2.00）和三次样条（2.36），且归一化TV排名（1.14）一致低于其他方法，因为平滑样条不需要穿过所有数据点，对dropout值不太敏感。
4. **纯样条拟合的局限**：不使用CFM学习的样条拟合EPR接近零，说明学习跨基因依赖的状态相关向量场不可或缺。
5. **CFM模型的优势**：CFM模型（FlowGRN和FlowGRN+）比纯样条拟合更能捕获跨基因依赖，但CFM+样条+投影的组合效果最佳。

### 7. 优点

- **解决问题聚焦**：清晰识别了FlowGRN的三个核心局限（dropout敏感、不连续性、依赖人工），并提出针对性解决方案。
- **方法创新**：将MMFM的多边缘思想引入GRN推断，结合了平滑样条和从加权LTSA启发的切空间投影，并利用Woodbury恒等式使高维投影计算可行。
- **自动化提升**：用伪时间分箱替代聚类，减少人工干预，提高可复现性。
- **消融实验系统**：对样条类型和投影方案进行系统消融，验证各组件贡献。
- **开源可用**：代码公开，便于复现和后续研究。

### 8. 不足与局限

- **流形假设限制**：平滑流形假设在分支数据集（如谱系分化）中可能不成立，ODE解的唯一性也限制了分支轨迹的建模，可能造成性能下降。
- **计算开销**：投影方案增加了训练时间，比FlowGRN更慢，影响可扩展性。
- **下游推断不足**：经dynGENIE3的固定推断流程可能未充分利用学习到的动态信息；GRNBOOST2与GENIE3的对比显示模型类和解释方法的选择会显著影响网络恢复效果。
- **性能权衡**：AUPRC提升不明显且方差大，说明全排名边的恢复不一定稳定优于基线，结论的泛化性需要更多验证。
- **实验覆盖有限**：仅在7个BEELINE实验数据集上评估，未包含大规模模拟数据，且未充分讨论参数敏感性（k、λ0、η、b等）。
- **轨迹评估单一**：仅以平滑度TV衡量轨迹质量，未评估轨迹的生物学保真度或与真实动态的偏差；更平滑不一定意味着更准确。

（完）
