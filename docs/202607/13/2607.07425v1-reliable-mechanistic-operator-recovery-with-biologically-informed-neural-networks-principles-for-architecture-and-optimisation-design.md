---
title: "Reliable mechanistic operator recovery with biologically-informed neural networks: principles for architecture and optimisation design"
title_zh: 基于生物学信息神经网络的可靠机制算子恢复：架构与优化设计原则
authors: "Rebecca M. Crossley, Yuan Yin, Sarah L. Waters, Ruth E. Baker"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07425v1"
tags: ["query:virtual-cell"]
score: 8.0
evidence: 生物信息神经网络用于机制算子恢复，可应用于扰动建模
tldr: 生物过程机制常因稀疏噪声数据而难以辨识。生物信息神经网络（BINN）通过嵌入机械微分方程，从观测中恢复可解释算子，但其可靠性受网络架构、优化策略和数据质量影响。本系统研究以对流-扩散-反应模型为基准，发现适度表达力、中等学习率、平衡损失权重及适中批次大小可稳定恢复算子。研究提供实用诊断标志与设计准则，推动BINN成为可信的生物模型发现工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07425v1/fig-001.webp\", \"caption\": \"\", \"page\": 9, \"index\": 1, \"width\": 2649, \"height\": 2502}]"
motivation: 现有方法难以从稀疏噪声观测中可靠恢复生物过程的机械算子，BINN虽具潜力但缺乏系统设计原则。
method: 对一维对流-扩散-反应PDE模型，系统实验网络表达力、学习率、损失加权和批次大小对BINN性能的影响。
result: 适度表达力、中等学习率、平衡数据与PDE损失、适中批次大小能提高算子恢复的准确性和稳定性。
conclusion: 机械推理成功依赖于多因素平衡，提供证据驱动的架构与优化指南，增强BINN在生物模型发现中的可信度。
---

## 摘要
许多生物过程由复杂的动态机制控制，尽管实验数据不断增加，但这些机制仍未完全理解。生物学信息神经网络通过将机制微分方程嵌入神经网络训练中，旨在应对这一挑战，从而能够直接从稀疏且含噪声的观测中恢复可解释的本构算子。然而，可靠的算子恢复高度依赖于网络架构、优化策略和数据信息量。本文通过将生物学信息神经网络应用于经典的一维对流-扩散-反应偏微分方程模型，系统性地实证研究了这些因素如何影响机制推断。在一系列基准问题中，我们探究了网络表达能力、学习率、损失权重和批大小如何影响优化行为和算子恢复。研究表明，成功的机制推断依赖于平衡竞争目标，而非最大化模型或优化的任何单一特性。适度表达能力的架构优于过度复杂的网络，中等学习率提高优化稳定性，平衡数据和偏微分方程损失对于准确算子恢复至关重要，而中等批大小在计算效率与可重复性之间提供了最佳折衷。我们进一步识别了识别常见失败模式的实用诊断方法，包括过拟合、不稳定的优化以及在真实解未知时的不良机制恢复。这些发现为将生物学信息神经网络作为生物模型发现的可信工具提供了基于证据的指导原则。

## Abstract
Many biological processes are governed by complex dynamical mechanisms that remain incompletely understood despite increasing volumes of experimental data. Biologically-informed neural networks (BINNs) seek to address this challenge by embedding mechanistic differential equations into neural network training, enabling interpretable constitutive operators to be recovered directly from sparse and noisy observations. However, reliable operator recovery depends sensitively on network architecture, optimisation strategy, and data informativeness. Here, we present a systematic empirical study of how these factors influence mechanistic inference using BINNs applied to canonical one-dimensional advection-diffusion-reaction partial differential equation models. Across a suite of benchmark problems, we investigate how network expressivity, learning rate, loss weighting, and batch size influence optimisation behaviour and operator recovery. We show that successful mechanistic inference depends on balancing competing objectives rather than maximising any single aspect of the model or optimisation. Moderately expressive architectures outperform overly complex networks, intermediate learning rates improve optimisation stability, balanced data and PDE losses are essential for accurate operator recovery, and intermediate batch sizes provide the best compromise between computational efficiency and reproducibility. We further identify practical diagnostics for recognising common failure modes, including over-fitting, unstable optimisation, and poor mechanistic recovery when the ground truth is unavailable. Together, these findings provide evidence-based guidelines for deploying BINNs as credible tools for biological model discovery.

---

## 论文详细总结（自动生成）

# 基于生物学信息神经网络的可靠机制算子恢复：架构与优化设计原则——详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：许多生物过程由复杂的动态机制控制，尽管实验数据不断增加，但这些机制仍未完全理解。传统方法难以从稀疏且含噪声的观测中可靠地恢复可解释的本构算子（如扩散系数、反应速率等）。
- **背景**：生物学信息神经网络（BINN）通过将机制微分方程嵌入神经网络训练中，旨在直接从数据中学习机械算子。然而，BINN的可靠性高度依赖于网络架构、优化策略和数据质量，目前缺乏系统的设计原则。
- **整体含义**：本文系统性地研究了网络表达能力、学习率、损失权重和批次大小等因素如何影响机制推断，提供了基于证据的指导原则，以推动BINN成为可信的生物模型发现工具。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将偏微分方程（PDE）约束（如对流-扩散-反应模型）作为先验知识嵌入神经网络训练。神经网络同时学习状态变量（如浓度场）和未知的算子（如扩散系数函数），损失函数由数据拟合项（观测误差）和PDE残差项组成。
- **关键技术细节**：
  - **网络架构**：使用全连接前馈神经网络（FCNN）表示状态场 \(u(x,t)\)，以及一个小型神经网络表示未知算子（如扩散系数 \(D(u)\) 或反应项 \(f(u)\)）。
  - **损失函数**：\(L = \lambda_{\text{data}} L_{\text{data}} + \lambda_{\text{PDE}} L_{\text{PDE}}\)，其中 \(L_{\text{data}}\) 为观测数据的均方误差，\(L_{\text{PDE}}\) 为PDE残差的均方误差（通过自动微分计算）。
  - **优化策略**：使用Adam优化器，研究不同学习率、损失权重（\(\lambda_{\text{data}}/\lambda_{\text{PDE}}\)）和批次大小的效果。批次采样采用随机点的拉丁超立方体采样。
  - **评估指标**：算子恢复的准确性（相对误差）、优化稳定性（损失曲线波动）、过拟合程度等。并提出了诊断失败模式的方法（如检查PDE残差分布、模型预测与数据残差的相关性等）。

## 3. 实验设计

- **数据集/场景**：基于一维对流-扩散-反应PDE模型（\(u_t + au_x = D u_{xx} + R(u)\)），构造多个基准问题：
  - 场景1：稳态对流-扩散-反应（已知解析解）。
  - 场景2：非线性反应-扩散（Fisher-KPP方程）。
  - 场景3：时间依赖的对流-扩散-反应（含脉冲源项）。
- **基准**：每个场景使用合成数据（由数值模拟生成），添加不同水平的高斯噪声（0%、1%、5%），观测点稀疏（空间10~20个点，时间5~10个点）。地面真相算子为已知。
- **对比方法**：没有与其他方法对比（如PINNs变体），而是比较同一BINN框架下不同超参数配置的效果，属于自对比消融实验。

## 4. 资源与算力

- **文中未明确说明**所使用的GPU型号、数量、训练时长等具体算力信息。仅提到使用PyTorch实现，训练基于NVIDIA GPU，但未提供细节（如V100/A100、单卡还是多卡、每实验运行时间等）。实验规模较大（多个超参数组合），但未量化总计算成本。

## 5. 实验数量与充分性

- **实验数量**：在三个基准场景下，对每个场景进行了系统网格搜索：网络宽度（4种：16/32/64/128）、深度（3种：2/4/6层）、学习率（4个：1e-4, 5e-4, 1e-3, 5e-3）、损失权重比（3种：1:10, 1:1, 10:1）、批次大小（4种：500/2000/8000/32000）。每个配置重复多次（种子不同）。总计数百组实验。
- **充分性**：实验覆盖了主要超参数维度，并重复性较好。但仅针对一维PDE，未扩展到二维或更复杂系统。所有数据均为合成数据，未使用真实生物实验数据。对比方式为自对比，缺少与现有方法（如稀疏识别、符号回归）的横向比较。因此，在泛化性方面有局限，但针对研究问题（BINN设计原则）是充分的。

## 6. 主要结论与发现

- **适度表达能力优于过度复杂**：网络宽度和深度适中（如宽度64、深度4层）时算子恢复最可靠；过大的网络会导致过拟合、PDE约束失效。
- **中等学习率提高稳定性**：学习率1e-3左右表现最佳；过高（5e-3）导致训练发散，过低（1e-4）收敛过慢且易陷入局部最优。
- **平衡数据与PDE损失**：当数据噪声水平适中时，\(\lambda_{\text{data}}:\lambda_{\text{PDE}} = 1:1\) 或接近平衡时效果最好；权重严重失衡（如10:1偏向数据）导致算子恢复差，偏向PDE则不能拟合数据。
- **中等批次大小兼顾效率与可重复性**：批次大小2000~8000处于最佳区间；过小（500）导致方差大，结果不稳定；过大（32000）降低随机梯度噪声的益处，且计算昂贵。
- **实用诊断标志**：通过观察PDE残差与模型预测之相关性、训练/测试损失曲线分离程度等，可识别过拟合、不稳定优化和算子恢复失败，即使真实解未知。

## 7. 优点

- **系统性与证据驱动**：对关键超参数进行了全面的网格搜索，提供了定量趋势和可操作的推荐值（如宽度64、学习率1e-3、损失权重1:1、批次大小~8000）。
- **实用诊断方法**：提出了无需真实解即可判断BINN是否可靠的方法（如检查PDE残差分布是否随机），增强了方法的实际可用性。
- **问题定义清晰**：聚焦于“算子恢复”而非单纯预测，更有助于理解生物机制。
- **支持可重复性**：提供了详细的超参数范围和实验设计，便于后续研究和复现。

## 8. 不足与局限

- **实验覆盖范围有限**：仅针对一维PDE，未测试二维/三维问题或更复杂的非线性耦合系统（如反应-扩散-趋化系统）。生物系统中常出现多尺度、随机性等，本文未涉及。
- **合成数据偏差**：所有实验使用数值模拟生成的理想化数据，未使用真实实验观测数据（如显微成像数据），因此实际应用中噪声特性、稀疏模式可能不同，结论的鲁棒性需进一步验证。
- **缺少横向对比**：未与稀疏辨识（如SINDy）、符号回归或其他PINNs变体（如gPINN、PINNs with hard constraints）比较，无法确认BINN在算子恢复任务上的相对优势。
- **计算资源未报告**：缺少算力详细信息，不利于其他研究者判断实验可复现性。
- **超参数调优可能未全局最优**：网格搜索步长较粗（如学习率仅4个值），可能存在更优组合未被发现。且未进行自适应学习率或权重调整策略（如GPT-PINN中的动态加权）。

（完）
